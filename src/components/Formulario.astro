---

---

<article class="formulario">

    <h1>Contacta con nosotros</h1>
    <p>Si quieres hacer una reserva, tienes alguna sugerencia o simplemente quieres saludar, escríbenos a:</p><strong>cafeoslo@cafeoslo.es</strong>
    <p>O través de este formulario:</p>

    <button class="btn__form">ABRIR FORMULARIO</button>

    <div class="formComplete">
        
        <h1>Envíanos un mensaje</h1>
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="title__icon">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12" />
        </svg>              
    
        <form class="form" action="">
            <div class="form__inputs">
                <label class="input__label">Email: *</label>
                <input required class="input__type" type="email">
            </div>

            <div class="form__inputs">
                <label for="" class="input__label">Nombre y apellidos: *</label>
                <input required class="input__type" type="text">
            </div>

            <div class="form__inputs">
                <label for="" class="input__label">Mensaje: *</label>
                <textarea required class="input__type input__type-textarea" id=""></textarea>
            </div>
            
            <p class="form__p">* Campos obligatorios</p>
            <input class="input__btn" type="submit" value="Enviar"> 
    
        </form>

    </div>

    <section class="notification">
        <div class="notification__title">
            <h5>Mensaje Enviado</h5>
            <img src="../../public/exito.png" alt="mensaje ok">
        </div>

        <div class="notificacionSlide">
            
        </div>
    </section>

</article>

<script>

    const formulario = document.querySelector(".formComplete")
    const btn = document.querySelector(".btn__form")
    const iconBtn = document.querySelector(".title__icon")
    const notification = document.querySelector(".notification")
    const form = document.querySelector(".form")
    const inputs = document.querySelectorAll(".input__type")

    btn.addEventListener("click", () => {
        formulario.classList.toggle("show")
    })

    iconBtn.addEventListener("click", () => {
        formulario.classList.remove("show")
    })

    form.addEventListener("submit", (e) => {
        e.preventDefault()

        formulario.classList.remove("show")
        notification.classList.add("notificationShow")
        inputs.forEach(input => {
            (input as HTMLInputElement).value = ""
        })        
               
        setTimeout(() => {
            notification.classList.remove("notificationShow")
        }, 4000)

    })
    

   
</script>

<style>
    .formulario{
        width: 100%;
        display: flex;
        flex-direction: column;
        background-color: #DED6C0;
        color: #D03B40;
        padding: 20px;
        align-items: center;
        justify-content: center;
        text-align: center;
    }

    .form__title{
        width: 100%;
        display: flex;
        flex-direction: row;
        justify-content: space-around;
        position: relative;

    }

    .title__icon{
        position: absolute;
        top: 20px;
        right: 2%;
        width: 30px;
        height: 30px;
        cursor: pointer;
    }
    
    .btn__form{
        cursor: pointer;
		padding: 10px;
		margin-top: 20px;
		border: 2px solid white;
		font-family: system-ui, sans-serif;
		border-radius: 10px;
		font-weight: bold;
        font-size: 15px;
		color: black;
		background-color:transparent;
		transition: all .3s ease;
    }

    .btn__form:hover{
		background-color:#D03B40;
        color: white;
        
	}

    .formComplete{
        width: 80%;
        height: 80%;
        position: fixed;
        top: 10%;
        left: 10%;
        padding: 30px;
        background-color: white;
        box-shadow: 0px 0px 10px black;
        border-radius: 5px;
        opacity: 0;
        visibility: hidden;
        transition: all .3s ease;
        
        display: flex;
        flex-direction: column;
        justify-content: start;
        align-items: center;
    }

    .form{
        width: 90%;
        height: 80%;
        border: 2px solid #D03B40;
        border-radius: 5px;
        margin-top: 5%;
        padding: 20px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 2%;

    }

    @media (max-width: 800px){
        .form{
            height: 100%;
        }
    }

    .form__inputs{
        width: 100%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 5px;
    }

    .input__btn{
        margin-top: 10px;
        padding: 5px;
        border: solid 2px #D03B40;
        color: black;
        font-family: system-ui, sans-serif;
        font-weight: bold;
        background-color: white;
        width: 80px;
        border-radius: 5px;
        cursor: pointer;
        transition: all .3s ease;
    }

    .input__btn:hover{
        border-color: black;
        background-color: #D03B40;
        color: white;
    }

    .input__type{
        width: 90%;
        padding: 10px;
        border-radius: 5px;
        border: 2px solid #DED6C0;
        font-size: 20px;
        font-weight: 500;
        text-align: center
    

    }

    .notification{
        position: fixed;
        top: 10px;
        right: 20px;
        background-color: white;
        border: 2px solid #D03B40;
        border-radius: 5px;
        padding: 10px;
        display: flex;
        flex-direction: column;
        transition: all .4s ease;
        transform: translateX(200%);
    }

    .notificationShow{
        transform: translateX(0);
    }

    .notification__title{
        display: flex;
        flex-direction: row;
        justify-content: center;
        align-items: center;
        gap: 15px;
    }

    .notification__title > img{
        width: 30px;
        height: 30px;
    }

    .notification__title > h5{
        margin: 0;
    }

    .show{
        opacity: 1;
        visibility: visible;
    }
</style>